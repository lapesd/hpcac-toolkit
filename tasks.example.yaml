tasks:
  - task_tag: "task1"
    setup_commands:
      - hostname
      - uptime
    run_commands:

  - task_tag: "task2"
    setup_commands:
    run_commands:
      - ls
      - mkdir task3_dir
      - ls
      - mkdir task3_dir
      - rm -rf task3_dir
      - ls


